<UserControl x:Class="HitsDownloadManager.Views.DownloadDetailPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="800">
    <Border BorderBrush="Gray" BorderThickness="1" Padding="10" Margin="5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <!-- URL -->
            <TextBlock Grid.Row="0" Grid.Column="0" Text="URL:" FontWeight="Bold" Margin="0,5"/>
            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding SelectedDownload.Url}" TextWrapping="Wrap" Margin="0,5"/>
            <!-- File Path -->
            <TextBlock Grid.Row="1" Grid.Column="0" Text="Save Path:" FontWeight="Bold" Margin="0,5"/>
            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding SelectedDownload.FilePath}" TextWrapping="Wrap" Margin="0,5"/>
            <!-- File Size -->
            <TextBlock Grid.Row="2" Grid.Column="0" Text="File Size:" FontWeight="Bold" Margin="0,5"/>
            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding SelectedDownload.FileSize}" Margin="0,5"/>
            <!-- Downloaded -->
            <TextBlock Grid.Row="3" Grid.Column="0" Text="Downloaded:" FontWeight="Bold" Margin="0,5"/>
            <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding SelectedDownload.Downloaded}" Margin="0,5"/>
            <!-- Speed -->
            <TextBlock Grid.Row="4" Grid.Column="0" Text="Speed:" FontWeight="Bold" Margin="0,5"/>
            <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding SelectedDownload.Speed}" Margin="0,5"/>
            <!-- Time Remaining -->
            <TextBlock Grid.Row="5" Grid.Column="0" Text="Time Remaining:" FontWeight="Bold" Margin="0,5"/>
            <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding SelectedDownload.ETA}" Margin="0,5"/>
            <!-- Segments -->
            <TextBlock Grid.Row="6" Grid.Column="0" Text="Segments:" FontWeight="Bold" Margin="0,5"/>
            <TextBlock Grid.Row="6" Grid.Column="1" Text="{Binding SelectedDownload.Segments}" Margin="0,5"/>
            <!-- Status/Error Log -->
            <TextBlock Grid.Row="7" Grid.Column="0" Text="Status Log:" FontWeight="Bold" Margin="0,5" VerticalAlignment="Top"/>
            <ScrollViewer Grid.Row="7" Grid.Column="1" VerticalScrollBarVisibility="Auto" Margin="0,5">
                <TextBlock Text="{Binding SelectedDownload.StatusLog}" TextWrapping="Wrap"/>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>
