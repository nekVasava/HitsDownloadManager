<UserControl x:Class="HitsDownloadManager.Views.AIAssistantPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="300">
    <Border BorderBrush="LightBlue" BorderThickness="2" CornerRadius="5" Padding="15" Background="#F5F5F5">
        <StackPanel>
            <!-- Header -->
            <TextBlock Text="🤖 AI Assistant" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
            <!-- Toggle Switch -->
            <StackPanel Orientation="Horizontal" Margin="0,5">
                <TextBlock Text="Enable AI Assistant:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <CheckBox IsChecked="{Binding IsAIEnabled}" 
                          Command="{Binding ToggleAICommand}"
                          VerticalAlignment="Center"/>
            </StackPanel>
            <!-- Status Indicator -->
            <StackPanel Orientation="Horizontal" Margin="0,10">
                <TextBlock Text="Status:" FontWeight="Bold" Margin="0,0,10,0"/>
                <Ellipse Width="12" Height="12" 
                         Fill="{Binding AIStatusColor}" 
                         VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBlock Text="{Binding AIStatus}" VerticalAlignment="Center"/>
            </StackPanel>
            <!-- AI Activity Log -->
            <TextBlock Text="Recent Activity:" FontWeight="Bold" Margin="0,10,0,5"/>
            <Border BorderBrush="Gray" BorderThickness="1" Background="White" Height="80">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <TextBlock Text="{Binding AIActivityLog}" 
                               TextWrapping="Wrap" 
                               Padding="5"
                               FontSize="11"/>
                </ScrollViewer>
            </Border>
            <!-- AI Command Input -->
            <TextBlock Text="Send Command:" FontWeight="Bold" Margin="0,10,0,5"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" 
                         Text="{Binding AICommandInput, UpdateSourceTrigger=PropertyChanged}" 
                         Height="25" 
                         VerticalContentAlignment="Center"
                         IsEnabled="{Binding IsAIEnabled}"/>
                <Button Grid.Column="1" 
                        Content="Send" 
                        Command="{Binding SendAICommandCommand}"
                        Width="60" 
                        Margin="5,0,0,0"
                        IsEnabled="{Binding IsAIEnabled}"/>
            </Grid>
        </StackPanel>
    </Border>
</UserControl>
